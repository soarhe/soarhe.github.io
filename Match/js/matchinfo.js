/**
 * Created by baidu on 2016/9/30.
 */

$(document).ready(docready);

function docready() {
    // 本地数据
    var flag = localStorage.getItem("flag");
    var homeid = localStorage.getItem("homeid");
    var awayid = localStorage.getItem("awayid");
    alert(flag + " " + homeid + " " + awayid);
    // 拉数据

    // 更新页面

}